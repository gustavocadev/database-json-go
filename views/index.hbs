<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>My go App</title>
</head>
<body class="bg-stone-900 text-white">
    <header>
        <nav>
            <ul>
                <li>
                    Home
                </li>
            </ul>
        </nav>
    </header>
    <form  method="POST" action="/newUser" class="container mx-auto p-4">
        <label for="name"class="block">ingrese su nombre</label>
        <input type="text" name="name" class="border border-black px-2 bg-gray-900" autocomplete="off" id="name" required>

        <label for="age" class="block">ingrese su edad</label>
        <input type="number" name="age" class="border border-black px-2 block bg-gray-900" autocomplete="off" id="age" required>

        <button type="submit" class="bg-blue-500 py-2 px-4 shadow-lg shadow-blue-400/50 text-white my-2 rounded" >
            Guardar
        </button>
    </form>
    <main>
        {{#each users }}
            <section class="grid grid-cols-3 place-items-center mx-auto my-4 w-11/12 border border-white p-4">
                <h1>mi edad es: {{ this.Age }}</h1>
                <h1>mi nombre es: {{ this.Name }}</h1>
                <div>
                    <a href="/update/{{ this.Id }}" class="bg-blue-500 px-4 py-2 rounded">Actualizar</a>
                    <a href="/delete/{{this.Id}}" class="bg-red-500 px-4 py-2 rounded"  >Eliminar</a>
                    
                </div>
            </section>
        {{/each}}
    </main>
</body>
</html>